// Shared font-family stacks. We use next/font (Cormorant + Mulish) exposed as
// CSS variables in layout.tsx, with the literal family names as fallback.
export const SERIF = "var(--font-cormorant), 'Cormorant', Georgia, serif";
export const SANS =
  "var(--font-mulish), 'Mulish', -apple-system, BlinkMacSystemFont, sans-serif";
